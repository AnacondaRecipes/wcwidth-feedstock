{% set name = "wcwidth" %}
{% set version = "0.2.14" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/wcwidth-{{ version }}.tar.gz
  sha256: 4d478375d31bc5395a3c55c40ccdf3354688364cd61c4f6adacaa9215d0b3605

build:
  number: 0
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  skip: true  # [py<36]

requirements:
  host:
    - python
    - pip
    - setuptools
    - wheel
  run:
    - python

test:
  source_files:
    - tests
    - bin/verify-table-integrity.py  # [py>311]
  imports:
    - wcwidth
  commands:
    - pip check
    - python -c "from importlib.metadata import version; assert(version('{{ name }}')=='{{ version }}')"
    - pytest -v tests
  requires:
    - pip
    - pytest >=7.4.2

about:
  home: https://github.com/jquast/wcwidth
  summary: Measures number of Terminal column cells of wide-character codes.
  license: MIT
  license_family: MIT
  license_file: LICENSE
  description: This library is mainly for CLI programs that carefully produce output for Terminals, or make pretend to be an emulator.
  doc_url: https://github.com/jquast/wcwidth/blob/master/docs/intro.rst
  dev_url: https://github.com/jquast/wcwidth

extra:
  recipe-maintainers:
    - ocefpaf
